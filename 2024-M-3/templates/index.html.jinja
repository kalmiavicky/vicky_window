<!DOCTYPE html>
<html lang="zh-Hant">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>機器學習芒果分析 - {% block title %}{% endblock %}</title>
    {% for css in external_stylesheets %}
    <link rel="stylesheet" href="{{ css }}">
    {% endfor %}
    <style>
        /* 新增的樣式，用於芒果圖片顯示 */
        .image-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
        }
        .image-item {
            max-width: 300px;
            margin-bottom: 20px;
        }
        .image-item img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
    </style>
</head>
<body>
    <div id="app">
        {% block content %}
        {{ app_entry | safe }}
        {% endblock %}
        
        {% block mango_images %}
        {% if images %}
        <h2>{{ market }} 芒果圖片</h2>
        <div class="image-container">
            {% for image in images %}
            <div class="image-item">
                <img src="{{ url_for('static', filename='images/' + image) }}" alt="{{ image }}">
            </div>
            {% endfor %}
        </div>
        {% endif %}
        {% endblock %}
    </div>
    <footer>
        {%- for script in scripts %}
            {{ script | safe }}
        {%- endfor %}
    </footer>
</body>
</html>